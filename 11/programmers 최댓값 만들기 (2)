function solution(numbers) {
    var answer = 0;
    for (i = 0; i < numbers.length; i++){
        for (a=0; a<numbers.length; a++){
            if (a==i){
                continue;
            }
            else if (answer < numbers[i] * numbers[a]){
                console.log("asd")
                answer = numbers[i] * numbers[a];
            }
            else{
                continue;
            }
        }
    }
    return answer;
}
