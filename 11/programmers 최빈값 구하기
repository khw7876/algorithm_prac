def solution(array):
    answer = 0
    arr = [0]*(max(array)+1)
    
    for i in array:
        if arr[i] == 0:
            arr[i] = 1
        else:
            arr[i] += 1
    
    answer = arr.index(max(arr))
    if arr.count(max(arr)) > 1:
        answer = -1
        
    return answer
