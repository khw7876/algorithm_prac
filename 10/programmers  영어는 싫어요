def solution(numbers):
    number_list = ["zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"]
    for i in number_list:
        numbers = numbers.replace(i, str(number_list.index(i)))
        
    return int(numbers)
